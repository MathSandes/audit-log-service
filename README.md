# audit-log-service

Node + TypeScript + Fastify + TypeORM + Postgres

Run locally:

- Install: npm install
- Dev: npm run dev
- Build: npm run build
- Start: npm run start

Services:
- POST /audit-logs
- GET /audit-logs?actorId=&entityType=&entityId=&limit=&offset=

